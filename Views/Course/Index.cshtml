@model IEnumerable<Course>

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-primary fw-bold">📚 Course List</h1>
        <a href="/Course/Create" class="btn btn-md btn-gradient-primary shadow-sm">➕ Create Course</a>
    </div>

    <form method="get" action="">
        <div class="d-flex justify-content-start align-items-center mb-3">
            <input type="search" name="searchString" class="form-control w-50 shadow-sm"
                placeholder="🔍 Search courses..." value="@ViewBag.searchString">
            <button type="submit" class="btn btn-primary ms-2">Search</button>
            <a href="/Course" class="btn btn-outline-secondary ms-2">Reset</a>
        </div>
    </form>



    <div class="table-responsive shadow-sm rounded">
        <table class="table table-bordered table-striped table-hover align-middle" id="courseTable">
            <thead class="bg-gradient-dark text-white">
                <tr>
                    <th style="width: 150px;">
                        <i class="bi bi-hash"></i> @Html.DisplayNameFor(m => m.CourseId)
                    </th>
                    <th>
                        <i class="bi bi-journal"></i> @Html.DisplayNameFor(m => m.CourseName)
                    </th>
                    <th>
                        <i class="bi bi-person"></i> @Html.DisplayNameFor(m => m.Teacher.NameSurname)
                    </th>

                    <th style="width: 200px; text-align: center;">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var course in @Model)
                {
                    <tr class="border-bottom">
                        <td class="text-center">@Html.DisplayFor(modelStudent => course.CourseId)</td>
                        <td>@Html.DisplayFor(modelStudent => course.CourseName)</td>
                        <td>@Html.DisplayFor(modelStudent => course.Teacher.NameSurname)</td>
                        <td class="text-center">
                            <a asp-controller="Course" asp-action="Edit" asp-route-id="@course.CourseId"
                                class="btn btn-sm btn-primary">✏️Edit</a>
                            <a asp-controller="Course" asp-action="Delete" asp-route-id="@course.CourseId"
                                class="btn btn-sm btn-danger">❌Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<style>
    .btn-gradient-primary {
        background: linear-gradient(45deg, #1d78d2, #47b8e0);
        color: white;
        border: none;
    }

    .btn-gradient-primary:hover {
        background: linear-gradient(45deg, #1663a8, #379db6);
        color: white;
    }

    .bg-gradient-dark {
        background: linear-gradient(45deg, #343a40, #495057);
    }

    .table-striped tbody tr:hover {
        background-color: #f8f9fa;
    }
</style>


